% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/promote.R
\name{qt_finalize_survey}
\alias{qt_finalize_survey}
\title{Finalize a Survey and Integrate Candidates into the Main Banks}
\usage{
qt_finalize_survey(
  survey_config_file,
  config = qt_config(),
  force_promote = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{survey_config_file}{Character string. Path to the survey config YAML.}

\item{config}{A \code{qt_config} object. Defaults to \code{qt_config()}.}

\item{force_promote}{Logical. If \code{TRUE}, always re-run
\code{\link[=qt_promote_candidates]{qt_promote_candidates()}} even when \verb{candidates/promoted/} already
contains files. Default \code{FALSE}.}

\item{dry_run}{Logical. If \code{TRUE}, run \code{\link[=qt_promote_candidates]{qt_promote_candidates()}} in
dry-run mode and skip bank integration. Default \code{FALSE}.}
}
\value{
Invisibly returns a named list summarising what was integrated:
\describe{
\item{\code{survey_id}}{Survey ID from the config.}
\item{\code{questions}}{List with \verb{$added} and \verb{$versioned} ID vectors.}
\item{\code{control}}{List with \verb{$added} ID vector.}
\item{\code{generated}}{List with \verb{$added} ID vector.}
\item{\code{surveys_used_updated}}{Character vector of all IDs whose
\code{surveys_used} was updated.}
}
}
\description{
The second step of the two-step survey finalization workflow.  Reads files
written by \code{\link[=qt_promote_candidates]{qt_promote_candidates()}} from \verb{candidates/promoted/} and
integrates them into the main question, control, and generated variable
banks.  Also appends the survey ID to \code{surveys_used} for every question,
module, control parameter, and generated variable referenced by the survey.
}
\details{
\code{\link[=qt_promote_candidates]{qt_promote_candidates()}} is called automatically if \verb{candidates/promoted/}
does not yet contain any \code{.yml} files.  Use \code{force_promote = TRUE} to
re-run it even when promoted files are already present.

\strong{Bank integration rules:}
\itemize{
\item{Entries tagged \verb{_bank_action: add} are appended as new variables.}
\item{Entries tagged \verb{_bank_action: version_update} are appended as a new
\code{versions} entry on the existing bank question (version-specific fields
only; base fields such as \code{storage_type} and \code{vargroup} are excluded).}
}
}
\seealso{
\code{\link[=qt_promote_candidates]{qt_promote_candidates()}}
}
