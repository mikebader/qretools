% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/promote.R
\name{qt_promote_candidates}
\alias{qt_promote_candidates}
\title{Sort Candidate Variables into Promoted and Unused Directories}
\usage{
qt_promote_candidates(
  survey_config_file,
  config = qt_config(),
  dry_run = FALSE
)
}
\arguments{
\item{survey_config_file}{Character string. Path to the survey
configuration YAML file.}

\item{config}{A \code{qt_config} object. Defaults to \code{qt_config()}.}

\item{dry_run}{Logical. If \code{TRUE}, report planned changes without writing
any files. Default \code{FALSE}.}
}
\value{
Invisibly returns a named list summarising what was sorted:
\describe{
\item{\code{survey_id}}{Survey ID from the config.}
\item{\code{questions}}{List with \verb{$promoted} and \verb{$relegated} ID vectors.}
\item{\code{control}}{List with \verb{$promoted} ID vector.}
\item{\code{generated}}{List with \verb{$promoted} ID vector.}
}
}
\description{
The first step of the two-step survey finalization workflow. Reads
candidate YAML files and sorts them into \verb{candidates/promoted/} and
\verb{candidates/unused/} based on whether they are referenced in the survey.
Does \strong{not} modify the main question, control, or generated variable banks.
}
\details{
\strong{Question candidates} (flat \code{.yml} files in \verb{candidates/}) are promoted
when they appear in the survey config with \code{source: candbank}.  Unreferenced
question candidates are relegated to \verb{candidates/unused/}.

\strong{Control-parameter and generated-variable candidates} (in
\verb{candidates/control/} and \verb{candidates/generated/}) are all promoted; there
is no unused bucket for these types.

When a promoted question candidate's \code{variable_id} already exists in the
main question bank the promoted entry is tagged \verb{_bank_action: version_update}
(only version-relevant fields will be appended as a new \code{versions} entry by
\code{\link[=qt_finalize_survey]{qt_finalize_survey()}}).  Genuinely new questions are tagged
\verb{_bank_action: add}.
}
\seealso{
\code{\link[=qt_finalize_survey]{qt_finalize_survey()}}
}
