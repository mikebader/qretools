% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variables.R
\name{variable-constructors}
\alias{variable-constructors}
\alias{qt_make_qvar}
\alias{qt_make_ctrlvar}
\alias{qt_make_genvar}
\title{Create Variable Objects}
\usage{
qt_make_qvar(var_data)

qt_make_ctrlvar(var_data)

qt_make_genvar(var_data)
}
\arguments{
\item{var_data}{List containing variable definition from YAML}
}
\value{
A variable object with appropriate class:
\itemize{
\item \code{qt_make_qvar()}: Returns object with class \code{c("qt_qvar", "qt_variable")}
\item \code{qt_make_ctrlvar()}: Returns object with class \code{c("qt_ctrlvar", "qt_variable")}
\item \code{qt_make_genvar()}: Returns object with class \code{c("qt_genvar", "qt_variable")}
}
}
\description{
Constructor functions for creating typed variable objects from YAML data.
These functions are strict and will fail if required fields are missing.
}
\section{Required Fields}{


\strong{All variable types require:}
\itemize{
\item \code{variable_id}: Variable identifier (string)
\item \code{title}: Short descriptive title (string)
\item \code{storage_type}: Data type (factor, integer, numeric, character, composite, multiple_response)
\item \code{vargroup}: Topic grouping (string)
\item \code{question_text}: Full question text as asked (string)
\item \code{surveys_used}: Survey instances where fielded (string vector, e.g., c("bas-2023", "bas-2024"))
}

\strong{Additional requirements by type:}
\itemize{
\item Factor variables: \code{value_label_id} (reference to value label set)
\item Control variables: \code{variable_role} must be "parameter"
\item Generated variables: \code{variable_role} must be "generated", requires \code{derivation_method}
\item Restricted variables: \code{restriction_reason} required if \code{restricted_access = TRUE}
\item Multi-part questions: \code{creates_variables} and \code{variable_parts} required if response_type is "select_all", "ranking", or "loop"
}
}

\examples{
\dontrun{
# Create a question variable
q_data <- list(
  variable_id = "nhd_sat",
  title = "Neighborhood Satisfaction",
  response_type = "factor",
  vargroup = "nhd",
  question_text = "How satisfied are you with your neighborhood?",
  value_label_id = "satisfied5",
  surveys_used = c("bas-2023", "bas-2024", "bas-2025")
)
q_var <- qt_make_qvar(q_data)

# Create a control parameter
ctrl_data <- list(
  variable_id = "JURISDICTION",
  title = "Survey Jurisdiction",
  storage_type = "character",
  vargroup = "svy",
  question_text = "Baltimore City or Baltimore County",
  variable_role = "parameter",
  surveys_used = c("bas-2020", "bas-2021", "bas-2022", "bas-2023", "bas-2024", "bas-2025")
)
ctrl_var <- qt_make_ctrlvar(ctrl_data)

# Create a generated variable
gen_data <- list(
  variable_id = "dem_income",
  title = "Household Income",
  storage_type = "factor",  # generated variables use storage_type directly
  vargroup = "dem",
  question_text = "Household income (combined)",
  value_label_id = "comb_income",
  variable_role = "generated",
  derived_from = c("dem_income_lo", "dem_income_hi"),
  derivation_method = "Combines low/high income based on 70k filter",
  surveys_used = c("bas-2023", "bas-2024", "bas-2025")
)
gen_var <- qt_make_genvar(gen_data)
}
}
