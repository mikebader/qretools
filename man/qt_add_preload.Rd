% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder.R
\name{qt_add_preload}
\alias{qt_add_preload}
\title{Add Preload Compute Items to a Survey}
\usage{
qt_add_preload(qre, ...)
}
\arguments{
\item{qre}{A \code{qt_qre} object.}

\item{...}{Compute item specs. Each must be created by \code{qt_add_compute()}
in spec mode (i.e., without a \code{qt_qre} first argument).}
}
\value{
Invisibly returns the modified \code{qt_qre} object.
}
\description{
Adds one or more compute items to the \code{preload} section of a survey
questionnaire. Only compute items (created with \code{qt_add_compute()} in
spec mode) are permitted in preload.
}
\examples{
\dontrun{
qre <- qt_qre("bas-2026", "BAS 2026", "draft") |>
  qt_add_preload(
    qt_add_compute(
      id          = "assign_condition",
      description = "Assign A/B condition",
      command     = "condition <- sample(c('A', 'B'), 1)",
      provides    = "condition"
    )
  )
}
}
