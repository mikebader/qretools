% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder.R
\name{qt_add_question}
\alias{qt_add_question}
\title{Add a Question Item}
\usage{
qt_add_question(
  .x,
  variable_id = NULL,
  source = "qbank",
  hide_question = FALSE,
  if_condition = NULL,
  programmer_note = NULL,
  version = NULL
)
}
\arguments{
\item{.x}{A \code{qt_qre} object (pipe mode) or a variable ID string (spec mode).}

\item{variable_id}{Character string. Variable key in the question bank.
Required in pipe mode; supply as \code{.x} in spec mode.}

\item{source}{Character string. Bank to use: \code{"qbank"} (default) or
\code{"candbank"}.}

\item{hide_question}{Logical. Hide question text? Useful inside
\code{qt_add_display_together()}. Default \code{FALSE}.}

\item{if_condition}{Character string or NULL. Survey-specific display logic
(R expression, e.g., \code{"dem_child == 1"}).}

\item{programmer_note}{Character string or NULL. Note to survey programmer.}

\item{version}{Character string or NULL. Version of the question to use when
the bank entry has a \code{versions} list. \code{NULL} (default) uses the last
(most recent) version. \code{"base"} uses only the original base entry.
Any other string selects the version with that \code{version_id}, applying all
earlier versions cumulatively (see \code{.qt_resolve_version()}).}
}
\value{
Pipe mode: invisibly returns the modified \code{qt_qre}. Spec mode:
returns an item spec list.
}
\description{
In pipe mode (\code{qre |> qt_add_question("dem_age")}), appends a question to
the top-level \code{questionnaire$items}. In spec mode
(\code{qt_add_question("dem_age")}), returns a plain item spec list for use
inside a container such as \code{qt_add_section()}.
}
\examples{
\dontrun{
# Pipe mode — use the most recent version (default)
qre |> qt_add_question("dem_age")

# Pipe mode — pin to a specific version
qre |> qt_add_question("dem_age", version = "v2")

# Spec mode (inside a section)
qt_add_section("demographics", "About You",
  qt_add_question("dem_age"),
  qt_add_question("dem_race", if_condition = "dem_age >= 18")
)
}
}
