% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder.R
\name{qt_process}
\alias{qt_process}
\alias{qt_process.qt_qre}
\alias{qt_process.character}
\title{Process a Survey Questionnaire}
\usage{
qt_process(x, ...)

\method{qt_process}{qt_qre}(x, yaml_path = .qt_default_yaml_path(x), config = NULL, ...)

\method{qt_process}{character}(x, config = NULL, ...)
}
\arguments{
\item{x}{A \code{qt_qre} object or a character string file path.}

\item{...}{Additional arguments (see methods).}

\item{yaml_path}{Character string or NULL. Path to write the YAML file
before processing. Defaults to a project-conventional path derived from
the survey ID and the \code{qt_config()} path settings:
\verb{<surveys>/<id>/design/survey-<id>.yml}. Pass \code{NULL} to suppress writing.}

\item{config}{A \code{qt_config} object or NULL. If NULL, calls \code{qt_config()}.}
}
\value{
A \code{qt_qreconfig} object (same as \code{qt_read_survey_config()}).
}
\description{
Expands a survey questionnaire into a flat \code{qt_qreconfig} object suitable
for analysis, codebook generation, and export. Accepts either a \code{qt_qre}
object (built programmatically) or a file path to a survey config YAML.
}
\details{
When called on a \code{qt_qre} object, the YAML is written to \code{yaml_path}
before processing. This keeps a documented YAML artifact on disk (for
version control and DDI export) while allowing the \code{qt_qre} object to be
passed directly without a separate write step.
}
