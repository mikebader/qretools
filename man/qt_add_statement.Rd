% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder.R
\name{qt_add_statement}
\alias{qt_add_statement}
\title{Add a Statement Item}
\usage{
qt_add_statement(
  .x = NULL,
  id = NULL,
  text = NULL,
  keep_with_next = FALSE,
  if_condition = NULL,
  display_class = NULL,
  programmer_note = NULL,
  note = NULL
)
}
\arguments{
\item{.x}{A \code{qt_qre} object (pipe mode), a statement ID string (spec mode),
or omitted entirely when calling in spec mode with named arguments.}

\item{id}{Character string or NULL. Unique statement identifier. If omitted
in both pipe and spec mode, an ID is auto-generated from a hash of the
text content.}

\item{text}{Character string. Statement text to display.}

\item{keep_with_next}{Logical. Display on same screen as next item?
Default \code{FALSE}.}

\item{if_condition}{Character string or NULL. Display logic (R expression).}

\item{display_class}{Character string or NULL. Display styling class.}

\item{programmer_note}{Character string or NULL. Note to survey programmer.}

\item{note}{Character string or NULL. Implementation notes.}
}
\value{
Pipe mode: invisibly returns modified \code{qt_qre}. Spec mode: item
spec list.
}
\description{
A statement is a block of text displayed to the respondent (e.g., an
introduction, transition, or closing message). In pipe mode, appends to
the top-level questionnaire items. In spec mode, returns an item spec list.
}
\examples{
\dontrun{
# Spec mode inside a logic block
qt_add_logic("nhd_sat <= 2",
  id = "nhd_problems_logic",
  condition = "nhd_sat <= 2",
  then = list(qt_add_question("nhd_prob_main")),
  otherwise = list(
    qt_add_statement("nhd_positive_affirm",
                     text = "Thank you for sharing that.")
  )
)
}
}
