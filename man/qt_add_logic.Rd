% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder.R
\name{qt_add_logic}
\alias{qt_add_logic}
\title{Add a Logic (Conditional) Block}
\usage{
qt_add_logic(
  .x,
  id = NULL,
  condition = NULL,
  then = list(),
  otherwise = list(),
  note = NULL,
  programmer_note = NULL
)
}
\arguments{
\item{.x}{A \code{qt_qre} object (pipe mode) or a logic block ID string (spec
mode). In spec mode, supply the ID as \code{.x} and all other arguments by
name.}

\item{id}{Character string. Unique identifier for the logic block.}

\item{condition}{Character string. R expression to evaluate
(e.g., \code{"dem_child == 1"}).}

\item{then}{List of item specs shown when \code{condition} is \code{TRUE}.}

\item{otherwise}{List of item specs shown when \code{condition} is \code{FALSE}.
Serializes as \verb{else:} in YAML.}

\item{note}{Character string or NULL. Implementation notes.}

\item{programmer_note}{Character string or NULL. Note to survey programmer.}
}
\value{
Pipe mode: invisibly returns modified \code{qt_qre}. Spec mode: item
spec list.
}
\description{
Routes survey flow based on an R expression. Items in \code{then} are shown
when the condition is true; items in \code{otherwise} are shown when false.
The \code{otherwise} argument serializes to \verb{else:} in YAML, which is what the
reader and DDI schema expect.
}
\examples{
\dontrun{
# Spec mode inside a section
qt_add_section("demographics", "About You",
  qt_add_question("dem_children"),
  qt_add_logic("dem_children_logic",
    condition = "dem_children == 1",
    then = list(
      qt_add_question("dem_children_n"),
      qt_add_question("dem_children_age_youngest")
    )
  )
)
}
}
